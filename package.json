{
  "name": "lawrenceandrews.co.uk",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build-css": "sass scss/:bin/css/",
    "build": "npm run clean && mkdir -p bin && cp -r index.html js images bin && npm run build-css",
    "clean": "rm -rf bin/",
    "watch": "sass scss/:bin/css/ --watch"
  },
  "devDependencies": {
    "sass": "^1.34.0"
  }
}
